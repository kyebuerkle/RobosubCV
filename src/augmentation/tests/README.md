#		Augmentation Tests

Unit tests for the augmentation subsystem.

##		Usage

`poetry run pytest` to run through unit tests

use `poetry run pytest --help` to show all the arguments
use `poetry run pytest --save` to save all the output images for showing